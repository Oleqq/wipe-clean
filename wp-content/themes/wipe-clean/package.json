{
  "name": "wipe-clean-theme",
  "version": "1.0.0",
  "private": true,
  "description": "Build pipeline for the Wipe Clean WordPress theme.",
  "scripts": {
    "build": "node ./scripts/build-assets.mjs",
    "watch": "node ./scripts/build-assets.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.11"
  }
}
